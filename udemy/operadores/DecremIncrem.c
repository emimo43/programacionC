#include <stdio.h>

int main()
{
    /*
    //Declaramos las variables
    Incremento
    int x = 6, y;
    y = x ++; // La variable x se va a incrementar en 1
    printf("EL valor de mi variable x es %d\n",x); //Aqui recien se incrementa el valor de x
    
   
    int x = 6, y;
    y = ++ x;//Aqui ya x vale 7 se incrementa el valor antes
    printf("El valor de mi variable x es %d\n",x);
    */

    /*
    Decremento
    int x = 6, y;
    y = x --;
    printf("EL valor de mi variable x es %d\n",x); //Aqui recien se decrementa el valor de x
    
    int x = 6, y;
    y = --x;
    printf("EL valor de mi variable x es %d\n",x); 
    */
    return 0;
}